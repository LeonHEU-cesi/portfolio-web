---
import type { SkillGroup } from '~/data/skills';
import type { Lang } from '~/i18n/ui';

interface Props {
  group: SkillGroup;
  lang: Lang;
}

const { group, lang } = Astro.props;
const label = lang === 'fr' ? group.label_fr : group.label_en;
---

<div>
  <h3 class="font-mono text-xs uppercase tracking-widest text-muted">{label}</h3>
  <ul class="mt-4 flex flex-wrap gap-2">
    {group.skills.map((skill) => (
      <li class="rounded border border-border bg-surface px-3 py-1.5 text-sm text-text transition-colors duration-200 ease-out hover:border-text">
        {skill}
      </li>
    ))}
  </ul>
</div>
